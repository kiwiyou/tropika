appenders:
  stdout:
    kind: console
    encoder:
      pattern: "[{d(%H:%M:%S)(utc)}][{h({l})}][{t}] {m}{n}"
root:
  level: debug
  appenders:
    - stdout
loggers:
  hyper:
    level: error
    additive: false 